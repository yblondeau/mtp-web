---
import { Image } from "astro:assets";
import { GALLERY_IMAGES } from "../lib/constants";
---

<hr class="divider-road" />
<section id="galerie" class="py-20 md:py-28">
  <div class="container">
    <header>
      <h2 class="font-display text-4xl md:text-6xl font-bold split-lines uppercase" data-reveal>
        Réalisations
      </h2>
      <p class="mt-4 text-muted" data-reveal>
        Interventions soignées et sécurisées, du devis à la réception de chantier.
      </p>
    </header>
    <div class="mt-10 grid grid-cols-2 md:grid-cols-4 gap-3" data-reveal>
      {GALLERY_IMAGES.map((photo) => (
        <Image
          src={photo.src}
          alt={photo.alt}
          width={640}
          class="rounded-xl border border-white/10 hover:border-accent transition-colors"
          loading="lazy"
        />
      ))}
    </div>
  </div>
</section>
